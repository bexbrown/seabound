import Leaderboard from "../components/Leaderboard/Leaderboard";

function LeaderboardPage({ player, setPlayer }) {
    return <Leaderboard player={player} setPlayer={setPlayer} />
}

export default LeaderboardPage;